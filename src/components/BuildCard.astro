---
import type { Build } from '../data/builds';

export interface Props {
  build: Build;
}

const { build } = Astro.props;

const accentVar = {
  budget: '--accent-budget',
  mid: '--accent-mid',
  high: '--accent-high',
  personal: '--accent-personal',
}[build.tier];

const partLabels: Record<string, string> = {
  cpu: 'CPU', cooler: "CPU Cooler", gpu: 'GPU', motherboard: 'Motherboard',
  ram: 'RAM', storage: 'Storage', psu: 'PSU',
  case: 'Case',
};
---

<div class="build-card">
  <div class="build-card-accent" style={`background: var(${accentVar})`}></div>
  <div class="build-card-header">
    <div>
      <h3 class="build-card-title">{build.title}</h3>
      <p class="build-card-desc">{build.description}</p>
    </div>
    <span class="build-card-price">${build.totalPrice}</span>
  </div>

    <div class="parts-list">
    {Object.entries(build.parts).map(([key, part]) => part && (
        <div class="part-row">
        <span class="part-label">{partLabels[key]}</span>
        <span class="part-name">{part.name}</span>
        <span class="part-price">${part.price}</span>
        </div>
    ))}
    </div>

    <div class="build-card-footer">
      <span class="last-updated">Prices may vary — listed prices are a current value baseline.</span>
      <a href={build.url} target="_blank" rel="noopener noreferrer" class="build-link">
        View full build on PCPartPicker ↗
      </a>
    </div>
</div>